.navbar.navbar-inverse.navbar-fixed-top
  .container-fluid
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#myNavbar', aria-expanded='false', aria-controls='navbar')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/')
        i.fa.fa-home &nbsp; TOBAGI
    .collapse.navbar-collapse(id="myNavbar")
      ul.nav.navbar-nav
        if (currentUser) 
          li
            a.glyphicon.glyphicon-list-alt(href='/monitor') 모니터링
          li 
            a.glyphicon.glyphicon-pencil(href='/babybooks') 육아일기
          li
            a.glyphicon.glyphicon-globe(href='/posts') 게시판
          li
            a.glyphicon.glyphicon-cog(href='/users/#{currentUser._id}/settings') 또바기설정
        else
          li
            a.glyphicon.glyphicon-list-alt(href='/login') 모니터링
          li 
            a.glyphicon.glyphicon-pencil(href='/login') 육아일기
          li
            a.glyphicon.glyphicon-globe(href='/login') 게시판
          li
            a.glyphicon.glyphicon-cog(href='/login') 또바기설정
      ul.nav.navbar-nav.navbar-right
        if (currentUser) 
          li
            a.glyphicon.glyphicon-user(href='/users/#{currentUser._id}') 프로필
          li
            a.glyphicon.glyphicon-log-out(href='/logout') 로그아웃
        else
          li
            a.glyphicon.glyphicon-log-in(href='/login') 로그인
          li
            a.glyphicon.glyphicon-user(href='/joinus') 회원가입
